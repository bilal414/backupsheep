"""Oracle Cloud compute node API package.

The project-level cloud URL include still needs one shared routing entry before
this endpoint is reachable; see the Oracle reliability handoff document.
"""
