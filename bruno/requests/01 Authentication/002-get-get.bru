meta {
  name: GET /api/v1/auth/logout/
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/auth/logout/
  body: none
  auth: none
}

headers {
}

script:pre-request {
  if (String(bru.getEnvVar("allowMutations")) !== "true") {
    throw new Error("Blocked by default: set allowMutations=true only after reviewing this exact request.");
  }
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `get-api-v1-auth-logout`

  No authentication required. Legacy/stateful GET; blocked unless `allowMutations=true`.

  Django action: `apps.api.v1.auth.views.APIAuthLogout.get`
  Source: `apps/api/v1/auth/views.py`
}
