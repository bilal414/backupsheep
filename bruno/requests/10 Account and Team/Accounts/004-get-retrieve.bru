meta {
  name: GET /api/v1/accounts/{{resourceId}}/
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/accounts/{{resourceId}}/
  body: none
  auth: none
}

headers {
  Authorization: Token {{apiToken}}
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `get-api-v1-accounts-resourceid`

  Requires `Authorization: Token <key>`. Read-only request.

  Django action: `apps.api.v1.account.views.CoreAccountView.retrieve`
  Source: `apps/api/v1/account/views.py`
}
