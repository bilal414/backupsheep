meta {
  name: POST /api/v1/accounts/{{resourceId}}/remove_membership/
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/v1/accounts/{{resourceId}}/remove_membership/
  body: json
  auth: none
}

headers {
  Authorization: Token {{apiToken}}
  Content-Type: application/json
}

body:json {
  {
    "membership_id": "{{membershipId}}"
  }
}

script:pre-request {
  if (String(bru.getEnvVar("allowMutations")) !== "true") {
    throw new Error("Blocked by default: set allowMutations=true only after reviewing this exact request.");
  }
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `post-api-v1-accounts-resourceid-remove-membership`

  Requires `Authorization: Token <key>`. State-changing request; blocked unless `allowMutations=true`.

  Django action: `apps.api.v1.account.views.CoreAccountView.remove_membership`
  Source: `apps/api/v1/account/views.py`
}
