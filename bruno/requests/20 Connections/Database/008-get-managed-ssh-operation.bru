meta {
  name: GET /api/v1/connections/database/{{resourceId}}/managed-ssh-operations/{{operationUuid}}/
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/api/v1/connections/database/{{resourceId}}/managed-ssh-operations/{{operationUuid}}/
  body: none
  auth: none
}

headers {
  Authorization: Token {{apiToken}}
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `get-api-v1-connections-database-resourceid-managed-ssh-operations-operationuuid`

  Requires `Authorization: Token <key>`. Read-only request.

  Django action: `apps.api.v1.connection.database.views.CoreDatabaseView.managed_ssh_operation`
  Source: `apps/api/v1/connection/database/views.py`
}
