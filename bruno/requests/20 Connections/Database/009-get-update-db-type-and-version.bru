meta {
  name: GET /api/v1/connections/database/{{resourceId}}/update_db_type_and_version/
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/api/v1/connections/database/{{resourceId}}/update_db_type_and_version/
  body: none
  auth: none
}

headers {
  Authorization: Token {{apiToken}}
}

script:pre-request {
  if (String(bru.getEnvVar("allowMutations")) !== "true") {
    throw new Error("Blocked by default: set allowMutations=true only after reviewing this exact request.");
  }
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `get-api-v1-connections-database-resourceid-update-db-type-and-version`

  Requires `Authorization: Token <key>`. Legacy/stateful GET; blocked unless `allowMutations=true`.

  Django action: `apps.api.v1.connection.database.views.CoreDatabaseView.update_db_type_and_version`
  Source: `apps/api/v1/connection/database/views.py`
}
