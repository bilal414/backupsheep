meta {
  name: GET /api/v1/connections/digitalocean/{{resourceId}}/validate/
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/api/v1/connections/digitalocean/{{resourceId}}/validate/
  body: none
  auth: none
}

headers {
  Authorization: Token {{apiToken}}
}

script:pre-request {
  if (String(bru.getEnvVar("allowMutations")) !== "true") {
    throw new Error("Blocked by default: set allowMutations=true only after reviewing this exact request.");
  }
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `get-api-v1-connections-digitalocean-resourceid-validate`

  Requires `Authorization: Token <key>`. Legacy/stateful GET; blocked unless `allowMutations=true`.

  Django action: `apps.api.v1.connection.digitalocean.views.CoreDigitalOceanView.validate`
  Source: `apps/api/v1/connection/digitalocean/views.py`
}
