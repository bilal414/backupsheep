meta {
  name: GET /api/v1/connections/google_cloud/{{resourceId}}/
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/v1/connections/google_cloud/{{resourceId}}/
  body: none
  auth: none
}

headers {
  Authorization: Token {{apiToken}}
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `get-api-v1-connections-google-cloud-resourceid`

  Requires `Authorization: Token <key>`. Read-only request.

  Django action: `apps.api.v1.connection.google_cloud.views.CoreGoogleCloudView.retrieve`
  Source: `apps/api/v1/connection/google_cloud/views.py`
}
