meta {
  name: PUT /api/v1/connections/lightsail/{{resourceId}}/
  type: http
  seq: 8
}

put {
  url: {{baseUrl}}/api/v1/connections/lightsail/{{resourceId}}/
  body: json
  auth: none
}

headers {
  Authorization: Token {{apiToken}}
  Content-Type: application/json
}

body:json {
  {
    "location": "{{locationId}}",
    "auth_lightsail": {
      "region": "replace-with-region-id"
    },
    "name": "Example resource",
    "notes": "Created from the BackupSheep Bruno collection"
  }
}

script:pre-request {
  if (String(bru.getEnvVar("allowMutations")) !== "true") {
    throw new Error("Blocked by default: set allowMutations=true only after reviewing this exact request.");
  }
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `put-api-v1-connections-lightsail-resourceid`

  Requires `Authorization: Token <key>`. State-changing request; blocked unless `allowMutations=true`.

  Django action: `apps.api.v1.connection.lightsail.views.CoreLightsailView.update`
  Source: `apps/api/v1/connection/lightsail/views.py`
}
