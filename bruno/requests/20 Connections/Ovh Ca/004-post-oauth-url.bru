meta {
  name: POST /api/v1/connections/ovh_ca/oauth_url/
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/v1/connections/ovh_ca/oauth_url/
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {}
}

script:pre-request {
  if (String(bru.getEnvVar("allowMutations")) !== "true") {
    throw new Error("Blocked by default: set allowMutations=true only after reviewing this exact request.");
  }
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `post-api-v1-connections-ovh-ca-oauth-url`

  Requires the authenticated browser session and its Django CSRF token; API-token authentication is intentionally rejected. State-changing request; blocked unless `allowMutations=true`.

  Django action: `apps.api.v1.connection.ovh_ca.views.CoreOVHCAView.oauth_url`
  Source: `apps/api/v1/connection/ovh_ca/views.py`
}
