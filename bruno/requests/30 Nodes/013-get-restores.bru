meta {
  name: GET /api/v1/nodes/{{resourceId}}/restores/
  type: http
  seq: 13
}

get {
  url: {{baseUrl}}/api/v1/nodes/{{resourceId}}/restores/
  body: none
  auth: none
}

headers {
  Authorization: Token {{apiToken}}
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `get-api-v1-nodes-resourceid-restores`

  Requires `Authorization: Token <key>`. Read-only request.

  Django action: `apps.api.v1.node.views.CoreNodeView.restores`
  Source: `apps/api/v1/node/views.py`
}
