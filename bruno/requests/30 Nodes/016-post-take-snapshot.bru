meta {
  name: POST /api/v1/nodes/{{resourceId}}/take_snapshot/
  type: http
  seq: 16
}

post {
  url: {{baseUrl}}/api/v1/nodes/{{resourceId}}/take_snapshot/
  body: json
  auth: none
}

headers {
  Authorization: Token {{apiToken}}
  Content-Type: application/json
  Idempotency-Key: {{idempotencyKey}}
}

body:json {
  {
    "notes": "On-demand backup from Bruno",
    "storage_point_ids": [
      "{{storageId}}"
    ],
    "request_id": "{{idempotencyKey}}"
  }
}

script:pre-request {
  if (String(bru.getEnvVar("allowMutations")) !== "true") {
    throw new Error("Blocked by default: set allowMutations=true only after reviewing this exact request.");
  }
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `post-api-v1-nodes-resourceid-take-snapshot`

  Requires `Authorization: Token <key>`. State-changing request; blocked unless `allowMutations=true`.

  Django action: `apps.api.v1.node.views.CoreNodeView.take_snapshot`
  Source: `apps/api/v1/node/views.py`
}
