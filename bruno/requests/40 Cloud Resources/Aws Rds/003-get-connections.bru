meta {
  name: GET /api/v1/clouds/aws_rds/connections/
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/clouds/aws_rds/connections/
  body: none
  auth: none
}

headers {
  Authorization: Token {{apiToken}}
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `get-api-v1-clouds-aws-rds-connections`

  Requires `Authorization: Token <key>`. Read-only request.

  Django action: `apps.api.v1.cloud.aws_rds.views.CoreCloudAWSRDSView.connections`
  Source: `apps/api/v1/cloud/aws_rds/views.py`
}
