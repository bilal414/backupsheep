meta {
  name: GET /api/v1/clouds/hetzner/connections/
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/clouds/hetzner/connections/
  body: none
  auth: none
}

headers {
  Authorization: Token {{apiToken}}
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `get-api-v1-clouds-hetzner-connections`

  Requires `Authorization: Token <key>`. Read-only request.

  Django action: `apps.api.v1.cloud.hetzner.views.CoreCloudHetznerView.connections`
  Source: `apps/api/v1/cloud/hetzner/views.py`
}
