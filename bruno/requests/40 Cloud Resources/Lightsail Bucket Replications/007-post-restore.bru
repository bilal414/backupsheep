meta {
  name: POST /api/v1/clouds/lightsail_bucket_replications/{{resourceId}}/restore/
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/v1/clouds/lightsail_bucket_replications/{{resourceId}}/restore/
  body: json
  auth: none
}

headers {
  Authorization: Token {{apiToken}}
  Content-Type: application/json
  Idempotency-Key: {{idempotencyKey}}
}

body:json {
  {
    "source_run_id": "{{runId}}",
    "restore_prefix": "restore-example/",
    "target_prefix": "restored/",
    "request_id": "{{idempotencyKey}}"
  }
}

script:pre-request {
  if (String(bru.getEnvVar("allowMutations")) !== "true") {
    throw new Error("Blocked by default: set allowMutations=true only after reviewing this exact request.");
  }
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `post-api-v1-clouds-lightsail-bucket-replications-resourceid-restore`

  Requires `Authorization: Token <key>`. State-changing request; blocked unless `allowMutations=true`.

  Django action: `apps.api.v1.cloud.lightsail_bucket_replication.views.CoreLightsailBucketReplicationView.restore`
  Source: `apps/api/v1/cloud/lightsail_bucket_replication/views.py`
}
