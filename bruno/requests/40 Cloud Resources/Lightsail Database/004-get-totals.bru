meta {
  name: GET /api/v1/clouds/lightsail_database/totals/
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/clouds/lightsail_database/totals/
  body: none
  auth: none
}

headers {
  Authorization: Token {{apiToken}}
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `get-api-v1-clouds-lightsail-database-totals`

  Requires `Authorization: Token <key>`. Read-only request.

  Django action: `apps.api.v1.cloud.lightsail_database.views.CoreCloudLightsailDatabaseView.totals`
  Source: `apps/api/v1/cloud/lightsail_database/views.py`
}
