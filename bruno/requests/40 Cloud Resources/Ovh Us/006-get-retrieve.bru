meta {
  name: GET /api/v1/clouds/ovh_us/{{resourceId}}/
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/v1/clouds/ovh_us/{{resourceId}}/
  body: none
  auth: none
}

headers {
  Authorization: Token {{apiToken}}
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `get-api-v1-clouds-ovh-us-resourceid`

  Requires `Authorization: Token <key>`. Read-only request.

  Django action: `apps.api.v1.cloud.ovh_us.views.CoreCloudOVHUSView.retrieve`
  Source: `apps/api/v1/cloud/ovh_us/views.py`
}
