meta {
  name: GET /api/v1/clouds/vultr/{{resourceId}}/automatic-backups/
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/api/v1/clouds/vultr/{{resourceId}}/automatic-backups/
  body: none
  auth: none
}

headers {
  Authorization: Token {{apiToken}}
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `get-api-v1-clouds-vultr-resourceid-automatic-backups`

  Requires `Authorization: Token <key>`. Read-only request.

  Django action: `apps.api.v1.cloud.vultr.views.CoreCloudVultrView.automatic_backups`
  Source: `apps/api/v1/cloud/vultr/views.py`
}
