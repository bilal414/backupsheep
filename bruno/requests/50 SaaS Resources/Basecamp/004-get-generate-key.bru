meta {
  name: GET /api/v1/saas/basecamp/generate_key/
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/saas/basecamp/generate_key/
  body: none
  auth: none
}

headers {
  Authorization: Token {{apiToken}}
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `get-api-v1-saas-basecamp-generate-key`

  Requires `Authorization: Token <key>`. Read-only request.

  Django action: `apps.api.v1.saas.basecamp.views.CoreBasecampView.generate_key`
  Source: `apps/api/v1/saas/basecamp/views.py`
}
