meta {
  name: GET /api/v1/volumes/lightsail/
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/volumes/lightsail/
  body: none
  auth: none
}

headers {
  Authorization: Token {{apiToken}}
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `get-api-v1-volumes-lightsail`

  Requires `Authorization: Token <key>`. Read-only request.

  Django action: `apps.api.v1.volume.lightsail.views.CoreVolumeLightsailView.list`
  Source: `apps/api/v1/volume/lightsail/views.py`
}
