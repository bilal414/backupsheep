meta {
  name: PATCH /api/v1/volumes/lightsail/{{resourceId}}/
  type: http
  seq: 7
}

patch {
  url: {{baseUrl}}/api/v1/volumes/lightsail/{{resourceId}}/
  body: json
  auth: none
}

headers {
  Authorization: Token {{apiToken}}
  Content-Type: application/json
}

body:json {
  {
    "name": "Updated example resource"
  }
}

script:pre-request {
  if (String(bru.getEnvVar("allowMutations")) !== "true") {
    throw new Error("Blocked by default: set allowMutations=true only after reviewing this exact request.");
  }
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `patch-api-v1-volumes-lightsail-resourceid`

  Requires `Authorization: Token <key>`. State-changing request; blocked unless `allowMutations=true`.

  Django action: `apps.api.v1.volume.lightsail.views.CoreVolumeLightsailView.partial_update`
  Source: `apps/api/v1/volume/lightsail/views.py`
}
