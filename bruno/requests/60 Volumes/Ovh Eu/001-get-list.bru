meta {
  name: GET /api/v1/volumes/ovh_eu/
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/volumes/ovh_eu/
  body: none
  auth: none
}

headers {
  Authorization: Token {{apiToken}}
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `get-api-v1-volumes-ovh-eu`

  Requires `Authorization: Token <key>`. Read-only request.

  Django action: `apps.api.v1.volume.ovh_eu.views.CoreVolumeOVHEUView.list`
  Source: `apps/api/v1/volume/ovh_eu/views.py`
}
