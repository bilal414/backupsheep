meta {
  name: GET /api/v1/volumes/ovh_eu/{{resourceId}}/
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/v1/volumes/ovh_eu/{{resourceId}}/
  body: none
  auth: none
}

headers {
  Authorization: Token {{apiToken}}
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `get-api-v1-volumes-ovh-eu-resourceid`

  Requires `Authorization: Token <key>`. Read-only request.

  Django action: `apps.api.v1.volume.ovh_eu.views.CoreVolumeOVHEUView.retrieve`
  Source: `apps/api/v1/volume/ovh_eu/views.py`
}
