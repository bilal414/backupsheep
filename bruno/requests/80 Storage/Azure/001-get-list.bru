meta {
  name: GET /api/v1/storage/azure/
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/storage/azure/
  body: none
  auth: none
}

headers {
  Authorization: Token {{apiToken}}
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `get-api-v1-storage-azure`

  Requires `Authorization: Token <key>`. Read-only request.

  Django action: `apps.api.v1.storage.azure.views.CoreStorageAzureView.list`
  Source: `apps/api/v1/storage/azure/views.py`
}
