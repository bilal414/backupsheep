meta {
  name: GET /api/v1/storage/backblaze_b2/{{resourceId}}/validate/
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/api/v1/storage/backblaze_b2/{{resourceId}}/validate/
  body: none
  auth: none
}

headers {
  Authorization: Token {{apiToken}}
}

script:pre-request {
  if (String(bru.getEnvVar("allowMutations")) !== "true") {
    throw new Error("Blocked by default: set allowMutations=true only after reviewing this exact request.");
  }
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `get-api-v1-storage-backblaze-b2-resourceid-validate`

  Requires `Authorization: Token <key>`. Legacy/stateful GET; blocked unless `allowMutations=true`.

  Django action: `apps.api.v1.storage.backblaze_b2.views.CoreStorageBackBlazeB2View.validate`
  Source: `apps/api/v1/storage/backblaze_b2/views.py`
}
