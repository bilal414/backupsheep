meta {
  name: GET /api/v1/storage/do_spaces/totals/
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/v1/storage/do_spaces/totals/
  body: none
  auth: none
}

headers {
  Authorization: Token {{apiToken}}
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `get-api-v1-storage-do-spaces-totals`

  Requires `Authorization: Token <key>`. Read-only request.

  Django action: `apps.api.v1.storage.do_spaces.views.CoreStorageDoSpacesView.totals`
  Source: `apps/api/v1/storage/do_spaces/views.py`
}
