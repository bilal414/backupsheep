meta {
  name: GET /api/v1/storage/filebase/regions/
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/storage/filebase/regions/
  body: none
  auth: none
}

headers {
  Authorization: Token {{apiToken}}
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `get-api-v1-storage-filebase-regions`

  Requires `Authorization: Token <key>`. Read-only request.

  Django action: `apps.api.v1.storage.filebase.views.CoreStorageFilebaseView.regions`
  Source: `apps/api/v1/storage/filebase/views.py`
}
