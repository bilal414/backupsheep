meta {
  name: GET /api/v1/storage/ionos/{{resourceId}}/
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/api/v1/storage/ionos/{{resourceId}}/
  body: none
  auth: none
}

headers {
  Authorization: Token {{apiToken}}
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `get-api-v1-storage-ionos-resourceid`

  Requires `Authorization: Token <key>`. Read-only request.

  Django action: `apps.api.v1.storage.ionos.views.CoreStorageIonosView.retrieve`
  Source: `apps/api/v1/storage/ionos/views.py`
}
