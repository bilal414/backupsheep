meta {
  name: GET /api/v1/storage/local/file/{{storedBackupId}}/
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/storage/local/file/{{storedBackupId}}/
  body: none
  auth: none
}

headers {
  Authorization: Token {{apiToken}}
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `get-api-v1-storage-local-file-storedbackupid`

  Requires `Authorization: Token <key>`. Read-only download or signed-download-URL request.

  Django action: `apps.api.v1.storage.local.views.LocalStorageFileDownloadView.get`
  Source: `apps/api/v1/storage/local/views.py`
}
