meta {
  name: POST /api/v1/backups/aws_rds/
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/backups/aws_rds/
  body: json
  auth: none
}

headers {
  Authorization: Token {{apiToken}}
  Content-Type: application/json
}

body:json {
  {
    "schedule": {
      "name": "Example resource",
      "timezone": "UTC",
      "notes": "Created from the BackupSheep Bruno collection",
      "node": "{{nodeId}}",
      "storage_points": [
        "replace-with-storage-points-id"
      ]
    },
    "notes": "Created from the BackupSheep Bruno collection",
    "unique_id": "replace-with-unique-id",
    "aws_rds": "replace-with-aws-rds-id"
  }
}

script:pre-request {
  if (String(bru.getEnvVar("allowMutations")) !== "true") {
    throw new Error("Blocked by default: set allowMutations=true only after reviewing this exact request.");
  }
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `post-api-v1-backups-aws-rds`

  Requires `Authorization: Token <key>`. State-changing request; blocked unless `allowMutations=true`.

  Django action: `apps.api.v1.backup.aws_rds.views.CoreAWSRDSBackupView.create`
  Source: `apps/api/v1/backup/aws_rds/views.py`
}
