meta {
  name: GET /api/v1/backups/basecamp/{{resourceId}}/download/
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/api/v1/backups/basecamp/{{resourceId}}/download/
  body: none
  auth: none
}

params:query {
  storage_point_id: {{storagePointId}}
}

headers {
  Authorization: Token {{apiToken}}
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `get-api-v1-backups-basecamp-resourceid-download`

  Requires `Authorization: Token <key>`. Read-only download or signed-download-URL request.

  Django action: `apps.api.v1.backup.basecamp.views.CoreBasecampBackupView.download`
  Source: `apps/api/v1/backup/basecamp/views.py`
}
