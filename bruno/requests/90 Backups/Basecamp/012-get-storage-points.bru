meta {
  name: GET /api/v1/backups/basecamp/{{resourceId}}/storage_points/
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/api/v1/backups/basecamp/{{resourceId}}/storage_points/
  body: none
  auth: none
}

headers {
  Authorization: Token {{apiToken}}
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `get-api-v1-backups-basecamp-resourceid-storage-points`

  Requires `Authorization: Token <key>`. Read-only request.

  Django action: `apps.api.v1.backup.basecamp.views.CoreBasecampBackupView.storage_points`
  Source: `apps/api/v1/backup/basecamp/views.py`
}
