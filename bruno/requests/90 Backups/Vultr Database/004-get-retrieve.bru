meta {
  name: GET /api/v1/backups/vultr_database/{{resourceId}}/
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/backups/vultr_database/{{resourceId}}/
  body: none
  auth: none
}

headers {
  Authorization: Token {{apiToken}}
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `get-api-v1-backups-vultr-database-resourceid`

  Requires `Authorization: Token <key>`. Read-only request.

  Django action: `apps.api.v1.backup.vultr_database.views.CoreVultrDatabaseBackupView.retrieve`
  Source: `apps/api/v1/backup/vultr_database/views.py`
}
