meta {
  name: GET /api/v1/stats/backups/
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/stats/backups/
  body: none
  auth: none
}

headers {
  Authorization: Token {{apiToken}}
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `get-api-v1-stats-backups`

  Requires `Authorization: Token <key>`. Read-only request.

  Django action: `apps.api.v1.stats.views.BackupActivityView.get`
  Source: `apps/api/v1/stats/views.py`
}
