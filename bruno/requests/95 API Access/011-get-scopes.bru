meta {
  name: GET /api/v1/tokens/scopes/
  type: http
  seq: 11
}

get {
  url: {{baseUrl}}/api/v1/tokens/scopes/
  body: none
  auth: none
}

headers {
  Authorization: Token {{apiToken}}
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `get-api-v1-tokens-scopes`

  Requires `Authorization: Token <key>`. Read-only request.

  Django action: `apps.api.v1.token.views.ApiTokenViewSet.scopes`
  Source: `apps/api/v1/token/views.py`
}
