meta {
  name: GET /api/v1/docs/swagger/
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/docs/swagger/
  body: none
  auth: none
}

headers {
  Authorization: Token {{apiToken}}
}

tests {
  test("request does not fail with an unhandled server error", function () {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Operation ID: `get-api-v1-docs-swagger`

  Requires `Authorization: Token <key>`. Read-only request.

  Django action: `apps.api.v1.docs.views.ApiSwaggerView.get`
  Source: `apps/api/v1/docs/views.py`
}
