# Preserve the historical delivery-acknowledgement timeout while a reviewed
# network-isolated transition broker is being attested. Transition models
# deliberately expose no network and import no current bootstrap definitions.
consumer_timeout = 90000000
